export enum Role {
  PBP = 'pbp',
  GM = 'gm',
}

export enum Status {
  DRAFT = 'draft',
  PENDING = 'pending',
  APPROVED = 'approved',
  REVIEW = 'review',
  EXCLUDED = 'excluded',
}

export enum Band {}

export enum Zone {}

export enum Policy {}

export enum Band {}

export enum Age {}
